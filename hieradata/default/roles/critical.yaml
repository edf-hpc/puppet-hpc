profiles:
  - profiles::cluster::common
  - profiles::firewall::base
  - profiles::network::base
  - profiles::dns::server
  - profiles::dns::client
  - profiles::nfs::exports
  - profiles::ntp::server
  - profiles::ssmtp::client
  - profiles::access::base
  - profiles::sudo::base

profiles::network::gw_connect:  'clusterloc'
sssd::case_sensitive: 'true'
certificates_owner: 'openldap'
